CONTEXT GATHERING PHASE: {props.phase}

Your job is to produce a focused context file that will guide implementation.

STEPS:
1. READ prd/GUILLOTINE_CLIENT_PLAN.md — find {props.phase} goals
2. READ prd/ETHEREUM_SPECS_REFERENCE.md — find relevant specs
3. List (ls) nethermind/src/Nethermind/Nethermind.Db/ — note key files
4. List /Users/williamcory/voltaire/packages/voltaire-zig/src/ — note relevant APIs
5. Read src/host.zig — understand the HostInterface
6. List ethereum-tests/ directories — note test fixture paths
7. WRITE docs/context/{props.phase}.md with paths and summaries
8. Run: git add docs/context/{props.phase}.md

{props.previousFeedback ? `PREVIOUS FEEDBACK: ${props.previousFeedback.reasoning}` : ""}
{props.reviewFeedback ? `REVIEW FEEDBACK: ${props.reviewFeedback.feedback}` : ""}
{props.failingTests ? `FAILING TESTS: ${props.failingTests}` : ""}

**REQUIRED OUTPUT** — You MUST end with this exact JSON structure:
```json
{"{"}
  "contextFilePath": "docs/context/{props.phase}.md",
  "nethermindFiles": ["file1.cs"],
  "specFiles": ["file1.py"],
  "voltaireApis": ["module.Type"],
  "existingZigFiles": ["src/host.zig"],
  "testFixtures": ["ethereum-tests/path"],
  "summary": "What was gathered"
{"}"}
```
DO NOT skip the JSON output. The workflow will fail without it.
