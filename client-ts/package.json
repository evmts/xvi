{
  "name": "guillotine-mini-client-ts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test:prepare": "node scripts/repairVoltairePeer.mjs",
    "test:rpc": "bun run test:prepare && vitest run rpc",
    "test": "bun run test:prepare && vitest run",
    "test:watch": "bun run test:prepare && vitest"
  },
  "dependencies": {
    "@tevm/voltaire": "0.2.27",
    "effect": "^3.12.0",
    "voltaire-effect": "^0.2.27"
  },
  "devDependencies": {
    "@effect/vitest": "^0.18.1",
    "prettier": "^3.6.2",
    "test": "file:./packages/test-shim",
    "typescript": "^5.9.3",
    "vitest": "^2.1.8"
  }
}
